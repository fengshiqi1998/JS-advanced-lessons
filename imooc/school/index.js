var klass = require('./class');// class不能作为命名

klass.add('Scott',['白富美','高富帅']);

exports.add = function(klasses){
	klasses.forEach(function(imte,inddex){
		var _klass = item;
		var teacherName = item.teacherName;
		var students = item.students;

		klass.add('Scott',['白富美','高富帅']);
	});
}